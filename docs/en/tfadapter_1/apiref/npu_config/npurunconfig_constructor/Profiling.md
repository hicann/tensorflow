# Profiling

## profiling_config

Profiling configuration. Before creating NPURunConfig, you can instantiate a ProfilingConfig class for profiling configuration. For details about the constructor of the ProfilingConfig class, see [ProfilingConfig Constructor](../profilingconfig_constructor.md).

Example:

```python
config = NPURunConfig(profiling_config=profiling_config)
```
